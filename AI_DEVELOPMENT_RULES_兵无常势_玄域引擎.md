# 兵无常势 / 玄域引擎 AI 开发规范

适用于 FluidWarfare、XuanYu Engine、SunWu Engine、qizheng-interplay 与 Codex 协作开发

版本：v1.0｜整理日期：2026-07-01

---

## 一、总原则

- 目标优先级：先保证《兵无常势》与相关原型能稳定落地，再逐步沉淀玄域引擎能力。不要为了“通用引擎”提前做过度抽象。
- 开发方式：采用“软架构、硬解耦”。命名、品牌、目录迁移可以分阶段推进，但代码职责边界必须严格。
- AI 协作定位：AI / Codex 负责执行明确的小步任务；ChatGPT 主要负责计划、审计、风险判断、任务拆分和必要的示例方案。
- 所有开发结论必须可验证：不能只说“完成了”，必须能用 build、test、commit、行数、文件清单和审计文档证明。

## 二、5 + 100 铁律

- 生产代码文件原则上不得超过 100 行。这是硬性红线，不是普通建议。
- 单目录核心文件原则上不超过 5 个。超过后应继续拆子目录，而不是把大量文件堆在同一层。
- 150～200 行只能算临时容忍区，不应成为正常设计目标。超过 200 行必须列入拆分计划。
- 新增文件时要优先保证职责清楚、小而完整；能一屏看完，就能一眼判断职责是否正确。
- 不得通过 partial、白名单、巨型静态类等方式绕过 100 行红线。

## 三、SRP 单一职责原则

- 一个文件只表达一个清晰职责。UI、输入、状态、渲染、命令、诊断、持久化不得混在同一个文件里。
- 一个函数不应承担完整流程的所有细节。流程可以保留，但步骤应拆成明确的小职责对象或小方法。
- 判断标准：打开一个文件，无需上下翻页，就应该知道它负责什么、不负责什么。
- 拆分不是为了文件数量好看，而是为了降低认知负担、减少 AI 误改范围、方便完整文件替换。

## 四、命名规范

- 类名、文件名、目录名必须描述具体职责，不得用模糊名称掩盖边界不清。
- 慎用或禁用垃圾桶命名：Manager、Helper、Utils、Processor、Handler、Controller、Service、Factory、Creator、Common、Shared、Core、Base、Misc。
- 不是绝对禁止这些词，而是禁止用它们承载多个不相关职责。
- 推荐命名方式：用具体对象 + 具体动作，例如 RiverPathGenerator、ViewportDragInput、SelectionInputRoute、CameraOrbitInput。

## 五、Git 与提交规范

- 每次代码操作必须 commit 并 push 到 GitHub 分支。即使结果有问题，也通过后续修复提交或回滚提交处理，不保留一堆未提交改动。
- 每轮提交应包含清晰 commit message，能看出本轮做了什么、为什么做。
- 分支必须明确。审计时以 GitHub 最新分支状态为准，不以口头描述或本地未提交状态为准。
- 较大改动前应建立备份分支或锚点提交，避免无法回退。

## 六、每轮完成汇报规范

- 完成汇报必须简洁、直接，让不懂技术的领导也能看懂。
- 必须说明：做了什么、解决了什么问题、现在进度如何、下一步做什么。
- 必须给出可审计指标：Build 结果、Test 结果、commit、branch、变更文件、行数变化、风险项、审计文档路径。
- 不要堆抽象术语；不要用“应该没问题”“理论上可以”代替验证结果。

## 七、复杂 Bug 排查流程

- 遇到难修、不明确、反复修不好的 bug，不允许继续猜根因或盲改代码。
- 标准流程：1）列范围文件；2）加 probe log / trace；3）复现一次；4）根据日志定位；5）修复；6）复测；7）收口探针。
- probe log / trace 字段尽量使用中文，结果描述也用中文，方便排查和复盘。
- 适用场景：Gizmo 拖动、Vulkan 渲染、输入仲裁、Preview / Commit 状态、性能卡顿、复杂 UI 状态流。

## 八、高频交互链路规范

- Preview 高频阶段只做轻量预览：更新场景预览、必要渲染、轻状态，不做重 UI 刷新。
- 拖动、框选、路径预览、地形刷子、实时变换等高频链路，不得刷新 Inspector、Diagnostics、PickSnapshot 等重负载面板。
- Commit / Cancel 低频阶段再写 WorldState、刷新 Inspector、刷新 Diagnostics、更新 PickSnapshot、记录最终状态。
- 性能问题优先检查高频链路是否误触发低频重操作。

## 九、EditorShell 与编辑器结构规范

- EditorShell 只做装配和路由，不做具体业务逻辑。
- EditorShell.axaml.cs 不得重新膨胀为大文件，不得用 partial 无限续命。
- UI、输入、命令、渲染、状态更新、诊断必须分流到明确职责文件。
- 长期目标：生产文件 <=100 行，目录白名单保持 0，GlobalUsings 不得无节制增长。

## 十、文件修改交付规范

- 需要修改代码时，优先提供对应文件的完整代码，不只给片段，除非明确要求只要片段或 diff。
- 新增、移动、拆分文件时，应同步更新相关文档，例如 file-tree、CHANGELOG、架构说明、审计文档。
- 每个新增模块都要说明职责、调用方、输入输出、是否影响现有行为。
- 不能靠大范围重置整个项目来解决局部问题，除非已经明确这是重构轮目标。

## 十一、项目边界规范

- 玄域引擎 / XuanYu Engine 是总技术品牌。
- 孙武引擎 / SunWu Engine 是战争、战略战术、战役模拟方向的子品牌。
- 兵无常势 / Fluid Warfare 是游戏 / 作品本体。
- 奇正相生 / qizheng-interplay 是前置验证原型，不应膨胀成完整引擎。
- 原型主要验证战斗可读性、单位朝向、火力扇区、路径规划、战斗日志、武器参数和无 HP 战斗模型雏形。

## 十二、战斗模型规范

- 不走传统 HP 血条扣血路线。战斗结果应由打击力、压制、士气、组织、伤亡、持续作战能力等因素共同产生。
- 打击力不是 DPS，而是单位把火力有效施加到目标上的综合能力。
- 参数要有因果链路，不要为了平衡主观乱填。武器、距离、地形、姿态、压制、命中、持续火力之间要能解释。
- 优先建立可读、可解释、可调试的模型，再逐步扩大复杂度。

## 十三、UI / 编辑器体验规范

- 工具型 UI 实用优先，不追求过度艺术化。重点区域要最大化，信息卡片只做辅助。
- 状态文本显示或隐藏时，不应造成明显布局跳动。
- 图标优先使用 SVG / Canvas / 矢量方案，不使用 emoji 充当正式图标。
- 编辑器相机交互优先接近 Blender 风格：中键旋转、Shift + 中键平移、滚轮缩放、Home 查看全部、小键盘句点聚焦所选。
- 后续应加入设置菜单和自定义键位，避免输入系统继续硬编码。

## 十四、Codex 执行前检查清单

- 本轮任务是否足够小，能否在有限文件范围内完成？
- 是否明确列出会修改哪些文件、新增哪些文件、删除哪些文件？
- 是否会造成任何文件超过 100 行，或目录超过 5 个核心文件？
- 是否有 build / test / 手动验证步骤？
- 是否需要 probe log，而不是直接猜测修复？
- 是否会触碰高频交互链路？如果会，Preview 和 Commit 是否分离？
- 是否准备好 commit、push 和审计文档？

## 十五、最终验收标准

- Build：0 Error，最好 0 Warning。
- Tests：明确通过数量，例如 625/625。
- 行数：列出关键文件修改前后行数，确认不突破 100 行红线。
- 结构：列出新增文件、删除文件、移动文件和职责归属。
- 行为：说明功能是否改变，旧行为是否保持。
- 风险：列出仍未解决或需要观察的问题。
- Git：给出 commit hash、branch、是否已 push。
- 文档：给出审计文档或开发记录路径。

## 总结

核心一句话：以《兵无常势》落地为目标，用 5 + 100 控制复杂度，用 SRP 和明确命名防止代码腐烂，用 Git 提交和审计文档保证过程可追踪，用 probe log 解决复杂 bug，所有高频交互必须轻量化，AI 只负责可验证的小步任务，不能盲改、不能堆大文件、不能让原型膨胀成失控引擎。

## 建议文件路径

```text
docs/AI_DEVELOPMENT_RULES.md
```
