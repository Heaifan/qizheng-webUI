# 2026-07-08 工作日志

## 重新熟悉 qizheng-mvp-pc-v0.3 项目（地貌编辑器原型）

### 项目定性
- 纯前端、无构建工具、原生 ES5 风格 JS（window.QZ 全局命名空间 + var）
- Canvas 2D 渲染，离屏 terrainCanvas 缓存整张地形，主 canvas 仅 drawImage 视口裁剪 + 网格
- 地图 320×512=163840 格，4px/格，Uint8Array 存地形 ID
- 画笔模式（Bresenham 画线 + 方形笔刷）/ 拖图模式（平移相机）
- Pointer Events，移动端触屏友好

### 架构分层（4 目录 × 5 文件，符合单目录 ≤5 红线）
- src/ 基础设施：config / state / canvas / loop / app（app.fuc_initApp 编排 8 步初始化链）
- map/ 地图：terrain / mapData / mapHit / mapDraw / mapBrush
- ui/ 界面：uiLayout / uiButtons / uiText / uiState / uiBind
- input/ 输入：pointerState / pointerMap / pointerBrush / pointerPan / pointerBind

### 关键状态
- 此目录尚未 git init（非 git 仓库），无 .git
- 无 file-tree.md / changelog.md（文档体系未建立）
- 无 .workbuddy 目录（本次已创建 .workbuddy/memory/）

### 发现的真实代码问题（待确认修复）
1. [严重] index.html 的 class 名与 style.css 完全不匹配：
   - HTML 用 brand/canvas-wrap/toolbar/section-title/button-row/terrain-dot
   - CSS 用 title/canvasWrap/groupTitle/btnGrid/dot，且 CSS 布局是 .main grid+panel 侧栏方案，HTML 是 header/main/footer 垂直方案，两套结构未对齐 → 样式基本不生效
2. [中] QZ.fuc_getLayoutName 在 uiLayout.js 和 uiText.js 重复定义；uiText.js 版本用 QZ.State.isPortrait，但 state.js 无此字段 → 永远返回"横屏"，平板判断丢失
3. [轻] 拖图(pan)模式下 lastCell 从不更新，格信息停在旧值

### 待办建议
- 先补齐 git init + GitHub 远程 + file-tree.md/changelog.md 文档基线
- 优先修 HTML/CSS class 不匹配（影响首屏布局）

## 项目线定性修正（17:18 用户确认）
- 用户确认：之前拿 qizheng-interplay(Vue/Electron) 旧线反证是错误参照
- 正确定性：旧版本=Vue/Pinia/Electron/TS 战斗原型；新版本=纯 HTML/原生 JS/Canvas 地貌编辑器（即本目录 qizheng-mvp-pc-v0.3）
- 本目录就是新版本本体，上一轮读出特征正确，审计结论有效
- 确立 R1/R2/R3 推进路线（详见 MEMORY.md）：
  - R1 新版本基线：git init/接 GitHub + file-tree.md + changelog.md + 记录为纯 HTML 线
  - R2 首屏布局：以 index.html 为准改 style.css，不大改 JS，保证手机竖屏/电脑横屏，Canvas 不塌陷
  - R3 状态与交互：合并 QZ.fuc_getLayoutName（保留 uiLayout.js 三态）、删重复、修拖图格信息、手机触屏测试
- 问题优先级：P0=HTML/CSS class 不匹配 → P1=getLayoutName 重复 → P2=拖图格信息不更新
- 本轮不动代码，先输出计划与文件清单（用户要求：先计划不直改）
